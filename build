#!/bin/sh

CWD=`pwd`

MAKEFILE='danillonunes.make'
DRUPALPATH='drupal'
TRANSLATIONS='pt-br'

LOCALSITESPATH='site'
PUBLICSITESPATH='default'

rm -rf $CWD/$DRUPALPATH
drush make --translations=$TRANSLATIONS $CWD/$MAKEFILE $CWD/$DRUPALPATH
rm -rf $CWD/$DRUPALPATH/sites/$PUBLICSITESPATH
ln -s $CWD/$LOCALSITESPATH $CWD/$DRUPALPATH/sites/$PUBLICSITESPATH
